<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.narwal.parvesh.findunfollowers"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:icon="@mipmap/ic_launcher_icon"
        android:theme="@style/AppTheme"
        tools:replace="android:icon, android:theme">

        <!--<meta-data android:name="AA_DB_NAME" android:value="twitter_database.db"/>-->
        <!--<meta-data android:name="AA_DB_VERSION" android:value="5"/>-->
        <!--<meta-data android:name="QUERY_LOG" android:value="true"/>-->
        <!--<meta-data android:name="DOMAIN_PACKAGE_NAME" android:value="com.narwal.parvesh.findunfollowers"/>-->

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".WebView_Handler"/>
        <activity android:name=".NonFollowers"/>
    </application>


</manifest>